[Unit]
Description=Flask Application
After=network.target

[Service]
User=root
WorkingDirectory=${app_path}
ExecStart=${app_path}/venv/bin/python3 main.py
Restart=always

[Install]
WantedBy=multi-user.target